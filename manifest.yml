---
applications:
- name: pipelineTemplate
  memory: 512M #The maximum memory to allocate to each application instance
  instances: 1 #The number of instances of the application to start
  host: pipelineTemplate #Hostname for app routing. Unique to domain
  path: pipelineTemplate.war
  services:
    - cloudant_PTIA    #Identifier for DB2 service, must be provisioned before push

