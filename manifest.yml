---
applications:
- name: myProject
  memory: 512M #The maximum memory to allocate to each application instance
  host: myProject #Hostname for app routing. Unique to domain
  path: myProject.war
  services:
    - Cloudant_myproject